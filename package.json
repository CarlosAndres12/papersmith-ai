{
  "name": "papersmith-ai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Papersmith AI \u2014 native Claude Code skills for paper ingestion and paper-proposal.",
  "scripts": {
    "test": "node --test \"tests/**/*.mjs\"",
    "test:node": "node --test \"tests/**/*.mjs\"",
    "test:py": "pytest",
    "test:all": "npm run test:node && npm run test:py",
    "setup": "npm install && python scripts/setup_env.py install && bash scripts/setup-harnesses.sh",
    "proposal-deliberation": "node skills/proposal-deliberation/engine/cli.mjs",
    "setup:harnesses": "bash scripts/setup-harnesses.sh",
    "setup:env": "python scripts/setup_env.py install",
    "clean:env": "python scripts/setup_env.py clean"
  },
  "dependencies": {
    "jiti": "^2.4.2",
    "typebox": "1.3.7"
  }
}
