"""The stdout characterization seal's fixture/normalization package.

Not a test module itself — `corpus.py` builds the fixture corpus, `normalize.py`
strips nondeterministic bytes before digesting. See
`openspec/changes/the-seal-before-the-cut/design.md`.
"""
