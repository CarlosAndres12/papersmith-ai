# Fixture revision r1

A committed, pre-Cut-3 scalar-shape revision fixture (design.md D5). Its
sha256 is what `tests/fixtures/admissibility/scalar.json`'s own
`revisionSha256` field was computed against; both files travel together.
