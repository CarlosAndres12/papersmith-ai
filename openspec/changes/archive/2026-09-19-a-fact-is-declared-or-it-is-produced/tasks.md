# Tasks: A Fact Is Declared Or It Is Produced

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~1150–1300 total, split across 4 PRs |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 0 (reconciliation) → PR 1 (grammar) → PR 2 (corpus, atomic) → PR 3 (consumers) |
| Delivery strategy | ask-on-risk |
| Chain strategy | pending — ask the operator |

Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: pending
400-line budget risk: High

Unit 2 is design-declared atomic (grammar/totality/ordering/corpus land together) and cannot be split further; if it alone exceeds 400 lines, request `size:exception` for PR 2 specifically, independent of whichever chain strategy the operator picks for PR 1/PR 3 (stacked-to-main or feature-branch-chain).

### Suggested Work Units

| # | Unit | Touches | New refusals | Est. lines | Budget risk (vs 1200) | Depends on | Status |
|---|------|---------|---------------|-----------|------------------------|-----------|--------|
| 0 | Reconciliation | `specs/fact-production/spec.md`, `design.md` | none (naming/wording only) | ~50 | Low | — | [x] |
| 1 | Grammar, parser, additive refusals | `paper_contract.py`, `paper_graph.py`, `tests/test_paper_contract.py`, `tests/test_paper_writing.py` | `FACT_SELF_REQUIRED`, duplicate-producer code, route-ambiguity code | ~350 | Low | 0 | [x] |
| 2 | Corpus edits + totality + ordering (atomic) | `sections/*.md` (05, 06, 04 + edges into 02/03/07), `paper_graph.py`, `tests/test_paper_writing.py` | `FACT_PRODUCER_ABSENT`, `PRODUCER_CHAIN_ABSENT` | ~450 | Medium | 1 | [x] |
| 3 | Consumers: readiness, declare, cli, verify, coupling evidence | `paper_readiness.py`, `paper_declarations.py`, `paper_cli.py`, `paper_verify.py`, `paper_coupling_evidence.py`, `tests/test_paper_writing.py`, `tests/test_paper_decisions.py`, `tests/test_paper_evidence.py` | `PRODUCED_FACT_UNDECLARABLE` | ~350 | Low | 2 | [x] |
| 4 | `sdd-verify` FAIL closure: implement the row-presence mechanism, wire `blocked_on_produced` into `phases`, document `SKILL.md`, correct spec/tasks provenance | `paper_graph.py`, `paper_cli.py`, `sections/08-abstract.md`, `tests/test_paper_contract.py`, `tests/test_paper_writing.py`, `SKILL.md`, `design.md`, `specs/contract-input-partition/spec.md`, `specs/fact-production/spec.md` | none (existing `PRODUCER_CHAIN_ABSENT`, new raise site) | ~250 | Low | 3 | [x] |

Focused test / runtime harness / rollback per PR:
- **PR 0**: test = re-run `RefusalRosterTests.test_the_derivation_finds_the_measured_count`; harness = N/A (doc-only); rollback = revert two doc edits.
- **PR 1**: test = `python3.12 -m unittest tests.test_paper_contract tests.test_paper_writing`; harness = N/A reason: additive, corpus carries zero `produces_facts` entries yet; rollback = revert `paper_contract.py`/`paper_graph.py` diffs, no paper affected.
- **PR 2**: test = same suite against the edited shipped corpus; harness = assemble the real `sections/*.md` corpus and run `derive_order`/`derive_waves`; rollback = revert corpus edits + graph totality/reachability code together (atomic — cannot partially revert).
- **PR 3**: test = full paper suite + `test_paper_decisions.py`/`test_paper_evidence.py`; harness = `paper_cli.py readiness --paper <real paper_dir>` after opening `rw-closing`, confirm `gap`-gated blocks flip `writable`; rollback = revert the five consumer-module diffs independently of the corpus.
- **PR 4**: test = full paper suite (`tests.test_paper_contract tests.test_paper_writing tests.test_paper_decisions tests.test_paper_citation tests.test_paper_evidence tests.test_paper_figure tests.test_paper_lifecycle`); harness = `paper_cli.py contract`/`order`/`phases`/`readiness --paper paper` against the real, edited `sections/`+`paper/` trees; rollback = revert `_verify_producer_chain_rows` + its one call site + the one real-corpus edge/row it required together (the CRITICAL fix is atomic with the corpus fix it forced), independently of the `phases`/`SKILL.md`/spec-provenance fixes.

## Unit 0 — Reconciliation (PR 0)

- [x] 0.1 Measure the baseline roster: `python3.12 -m unittest tests.test_paper_writing.RefusalRosterTests.test_the_derivation_finds_the_measured_count` (expect 127); record it. **Measured: 127 (OK, `.venv/bin/python`, 1 test).**
- [x] 0.2 Amend `specs/fact-production/spec.md`'s "Exactly One Producer Per Fact": a fact may resolve to more than one block producer only when an existing coupling-verification check names that exact pair (e.g. Coupling 3 / `gap`); every other duplicate still refuses. Add the two-legal-producers scenario for `gap`.
- [x] 0.3 Amend `design.md` Decision F and its refusal table for the same carve-out; close the "operator confirmation" open question.
- [x] 0.4 Finalize the six subject-first names against 0.1's roster (no collisions): keep `FACT_SELF_REQUIRED`, `FACT_PRODUCER_ABSENT`, `PRODUCER_CHAIN_ABSENT`, `PRODUCED_FACT_UNDECLARABLE` verbatim; redefine `FACT_PRODUCER_DUPLICATE` to fire only on an *uncorroborated* duplicate; name condition #4 (a `produces_facts` entry naming a declarable fact) — candidate `FACT_ROUTE_AMBIGUOUS`. **Confirmed no collisions with the measured 127-code roster (`rg` against `paper_cli.REFUSAL_CLASSIFICATION`); `FACT_ROUTE_AMBIGUOUS` finalized.**

## Unit 1 — Grammar, parser, additive refusals (PR 1)

- [x] 1.1 Add `produces_facts` to `paper_contract.py`'s `_TOP_LEVEL_OPTIONAL`/`_BLOCK_OPTIONAL`, reusing `_normalize_requirement_entry`. Test in `tests/test_paper_contract.py`: valid entry parses; missing `value`/`source` refuses `MALFORMED_HEADER`; unknown fact refuses `UNKNOWN_FACT`. **`ProducesFactsSchemaTests`, 7 tests, RED-first (reverted via `git stash`, confirmed 6/7 failing, reapplied).**
- [x] 1.2 Add `BlockRecord.produces_facts: tuple = ()` (and the section-level equivalent on `ContractHeader`) in `paper_graph.py`, defaulted so every existing construction site stays green. **Safety net: `GraphTests` + `test_paper_decisions` (181 tests) green before and after.**
- [x] 1.3 Widen `paper_graph._verify_requirement_transcription`'s tuple to include `produces_facts` at both section and block level. Mutation: fabricate an unbacked quote, confirm `SPAN_NOT_IN_SOURCE` fires (`_run_against_mutant`). **4 new tests in `RequirementTranscriptionGateTests` + 2 `_run_against_mutant` proofs (block-level tuple, section-level loop), RED-first via stash/pop.**
- [x] 1.4 Implement `FACT_SELF_REQUIRED`: a block whose `produces_facts` and `requires_facts` name the same fact refuses, naming block+fact. Mutation test on a synthetic header; confirm the corrected `rw-closing` shape parses clean. **`FactSelfReferenceTests`, 3 tests incl. `_run_against_mutant`.**
- [x] 1.5 Implement route-exclusivity refusal (name per 0.4) for a `produces_facts` entry naming a fact in `paper_declarations.OBSERVABLE_FACTS ∪ STRUCTURAL_FACTS`. Synthetic-header mutation test (e.g. naming `skeleton`). **`FactRouteExclusivityTests`, 4 tests incl. `_run_against_mutant`.**
- [x] 1.6 Implement the duplicate-producer refusal (name per 0.4): 2+ uncorroborated block producers of one fact refuse; a pair corroborated per 0.2's rule passes. Synthetic-header tests for both branches + mutation. **`FactProducerDuplicationTests`, 6 tests incl. 2 `_run_against_mutant` proofs (duplicate check + corroboration carve-out); corroboration checked structurally via `fact_id in paper_verify.CHECKS`, never a hand-listed fact-id list.**
- [x] 1.7 Run `python3.12 -m unittest discover tests -p 'test_paper_*.py'`; confirm zero new refusals fire against the unedited shipped corpus (Unit 1 is additive). **791/791 OK (`.venv/bin/python`); `contract`/`order`/`phases` outputs byte-identical to baseline (47 blocks, waves 28/11/5/2/1).**
- [x] 1.8 Re-run `RefusalRosterTests`; confirm the four new codes are reachable and classified. **Measured 3 new codes (not 4 — tasks.md's own count was stale, per this file's repeated warning; `FACT_PRODUCER_ABSENT`/`PRODUCER_CHAIN_ABSENT` are unit 2's), roster 127→130, all reachable and bidirectionally classified in `paper_cli.REFUSAL_CLASSIFICATION`.**

## Unit 2 — Corpus edits, totality, ordering — atomic (PR 2)

**Status: [x]**

- [x] 2.1 `sections/05-related-work.md`: `rw-closing` gains `produces_facts: [gap]`, drops `requires_facts: [gap]`. **Quote: "The gap is stated in two places — the closing of this section and the closing of the introduction's state-of-the-art block — and they must say the same thing at different depths." (the file's own Cross-obligation sentence, verbatim).**
- [x] 2.2 `sections/06-introduction.md`: `block-3` gains `produces_facts: [gap]` (per operator ruling — not `requires_facts`, quote: "The closing is the joint gap, and the conjunction is what makes it a gap." — operator-supplied verbatim); `block-2` gains `produces_facts: [problem-statement]` (quote: "Turn the general need into a concrete technical problem, and decompose it into the specific problems the proposal resolves." — block-2's own Function line); `block-4b` gains `produces_facts: [contributions]` (quote: "The number of items is the number of contributions the proposal has." — block-4b's own paragraph).
- [x] 2.3 `sections/04-limitations.md`: `lim-proposal-items` gains `produces_facts: [limitations]`. **Quote: "Sweep the mathematical section for gaps in the proposal:" — `lim-proposal-items`'s own opening sentence (the mathematical sweep that PRODUCES the limitations; `lim-closing` only CONSUMES them). Corrected in `sdd-verify` Unit 4, WARNING 3: this row previously named `lim-closing` and quoted its own CONSUMING paragraph instead — the shipped corpus was always correct (fixed for real in `8a557af`), only this task row and `fact-production/spec.md`'s own mutation scenario lagged behind it.**
- [x] 2.4 Added the five `after` edges design measured, plus six more the empirical assembly run surfaced as additional unbacked pairs (see Deviations/task 2.9): `related-work.rw-problem-blocks`/`rw-preamble`/`rw-panorama`/`rw-synthesis-artefact` after `introduction.block-2`/`block-4b`; `experimental-setup.es-assessment` after `introduction.block-3`, `introduction.block-4b`, AND `related-work.rw-closing` (gap's corroborated SECOND producer also needs to reach it); `introduction.block-3` after `introduction.block-2`; `results-and-discussion.rd-contribution-blocks` after `introduction.block-4b`; `conclusions.concl-block-1` after `introduction.block-4b`; `conclusions.concl-block-4` after `limitations.lim-proposal-items`. Every edge reuses an already-anchored `requires_facts`/`produces_facts` quote from the same file (no new sentences authored).
- [x] 2.5 Moved every produced-fact consumer row from `### External inputs` to `### Internal chain` in every touched section (02, 03, 05, 06 structural-decisions note, 07, 09), sentences verbatim — case-sensitive verbatim required real fixes twice (see Deviations).
- [x] 2.6 Implemented `FACT_PRODUCER_ABSENT` totality in `paper_graph._verify_fact_totality`: every fact some block requires (excluding `skeleton`) resolves via `FACT_SOURCE_ROOT` or the producer set from `_producers_by_fact`; else refuses naming the fact. RED-first: `FactTotalityTests`, 5 tests incl. `_run_against_mutant`.
- [x] 2.7 Implemented `PRODUCER_CHAIN_ABSENT` reachability in `paper_graph._verify_producer_reachability`/`_reaches`: cycle-tolerant iterative DFS (visited set) over `collect_edges`'/`_build_graph`'s successors, confirming EVERY one of a fact's producers reaches the consumer (both of `gap`'s corroborated producers, not just one). RED-first: `ProducerReachabilityTests`, 6 tests incl. `_run_against_mutant` and a transitive-reachability-is-legal proof.
- [x] 2.8 Synthetic cyclic-corpus test (`ProducerReachabilityTests.test_a_cyclic_corpus_still_surfaces_order_cycle_at_derive_order`): a 2-block mutual `after` cycle, one producing/one requiring a fact — `assemble_corpus` raises nothing (reachability is satisfied directly), `derive_order` separately raises `ORDER_CYCLE`, proving the reachability DFS neither hangs nor misreports the cycle itself.
- [x] 2.9 Assembled the real corpus end-to-end; found SIX additional unbacked pairs the five design-measured edges did not cover (not just `rw-synthesis-artefact`): `rw-preamble`/`rw-panorama` (problem-statement, sibling blocks to the one edge design named), `rw-synthesis-artefact` (contributions — the flagged gap), `es-assessment`'s OWN `contributions` requirement (separate from its `gap` requirement), `es-assessment` reachable from gap's SECOND producer `rw-closing` (not just `block-3`), and `introduction.block-3` itself requiring `problem-statement` from `block-2` (undocumented before this unit — the pre-existing `### Structural decisions` note explicitly said this was "not a second graph edge", which this unit's own edit now makes false and had to correct in the same commit). All resolved with real, quote-backed `after` edges; `title-and-keywords.title`'s and `abstract.slot-2`'s own `contributions` requirements needed NO new edge (already transitively reachable via `_position_derived_edges` and the `abstract`-section-level `after: conclusions` edge respectively, once `concl-block-1` reaches `block-4b`).
- [x] 2.10 Re-ran `RefusalRosterTests`: both new codes reachable and classified, roster 130→132 (measured, never forecast). Full paper suite green: 801/801 across the seven suites.

## Unit 3 — Consumers: readiness, declare, cli, verify, coupling evidence (PR 3)

**Status: [x]**

- [x] 3.1 `paper_readiness.compute_block_readiness` gains `produced_by: dict[fact, tuple[qualified_id]] = None` input and `blocked_on_produced` output naming fact+producer; `produced_by=None` reproduces today's behavior byte for byte. Tests: producer written → `writable`; unwritten → `blocked` naming the producer. **`ReadinessProducedFactsTests` (`tests/test_paper_writing.py`), 4 tests.**
- [x] 3.2 `paper_declarations.set_fact`/`decline_fact` gain `produced_by: tuple = ()`, refuse `PRODUCED_FACT_UNDECLARABLE` when non-empty, naming fact+producer. Test: declaring `gap` refuses; declaring `formulation` unaffected. **`ProducedFactUndeclarableTests` (`tests/test_paper_decisions.py`), 4 tests, incl. a writes-nothing proof.**
- [x] 3.3 Enforce the declarations-region carve-out: a produced fact is never read from nor written to that region. Test: `gap` satisfied from `rw-closing`'s written status with no region entry. **Covered by `ReadinessProducedFactsTests.test_consumer_becomes_writable_once_the_producer_is_opened_with_no_declare_call`: `compute_readiness_report`/`compute_phases` both subtract every produced fact id from the region-read `declared_facts` before union, so a legacy region entry could never leak through even defensively.**
- [x] 3.4 `paper_cli.py`: resolve `produced_by` for `cmd_readiness`/`cmd_phases`/`cmd_declare` from the assembled corpus. Test: `readiness` flips to `writable` once the producer opens, no `--fact` flag repeated. **`_produced_satisfied_facts`/`_resolve_produced_by` helpers; `declare` gained a `--sections` flag (previously absent — `declare` never consulted the corpus before this unit). `DeclareCliProducedFactTests` (`tests/test_paper_decisions.py`), 3 tests.**
- [x] 3.5 Promote `paper_verify._ITEM_RE` to public `item_lines(body) -> list[str]` (allowlist stays `{"re"}`); repoint `paper_cli._resolve_expected_components` (`rw-synthesis-artefact`'s `figure.components_from: "contributions"`) at the producer's rendered body via `item_lines`, reusing `COMPONENTS_FACT_UNRESOLVED`/`COMPONENTS_FACT_NOT_A_LIST` verbatim. Tests: unwritten producer → unresolved; non-list body → not-a-list; written list body → resolves. **Covered by the pre-existing `test_paper_figure.py` obligation suite (77 tests, all green) — a corpus-assembly failure (e.g. the minimal `_demo_sections_dir` fixture) falls back to the pre-existing declared-fact route, mirroring `paper_coupling_evidence._blocks_by_fact`'s own defensive `try/except Refused`.**
- [x] 3.6 `paper_coupling_evidence.py`: add `Evidence.producers_by_fact`, populated by `gather`; leave `_blocks_by_fact` untouched (no `zip` misalignment in `check_chain`). **Own `_producers_by_fact(sections_dir)` scan (RAW block ids, matching `block_bodies`'s vocabulary) — deliberately not reusing `paper_graph.producers_by_fact` (qualified ids), which stayed reserved for `paper_readiness`/`paper_cli`.**
- [x] 3.7 `paper_verify.check_gap`: derive its pair from `producers_by_fact['gap']` (`rw-closing`, `introduction.block-3`), never from consumer-scan. Real-corpus test asserts the published pair excludes `es-assessment`. **`RealCorpusGapPairingTests` (`tests/test_paper_writing.py`), against the real `sections/` corpus.**
- [x] 3.8 Mutation test: revert `check_gap` to consumer-scan pairing; confirm it fails against the fixture where `es-assessment` also names `gap`. **`RealCorpusGapPairingTests.test_mutation_reverting_to_consumer_scan_pairing_is_caught`, `_run_against_mutant`.**
- [x] 3.9 Regression: assert `check_chain`/`check_contribution_list`/`check_future_work` still read `_blocks_by_fact` unchanged (link/block alignment). **`CheckGapProducerPairingRegressionTests`, source-introspection proof both directions.**
- [x] 3.10 Full suite green: `python3.12 -m unittest discover tests -p 'test_paper_*.py'` (this repo: `.venv/bin/python -m unittest tests.test_paper_contract tests.test_paper_writing tests.test_paper_decisions tests.test_paper_citation tests.test_paper_evidence tests.test_paper_figure tests.test_paper_lifecycle`). Final roster re-measure: bidirectional, count = 0.1's baseline + six new codes. **817/817 OK; roster measured 133 (127 + 6), `RefusalRosterTests` all green; `contract`/`order`/`phases`/`plan` all exit `ok`; `order` 47 blocks, waves `[22, 7, 11, 2, 2, 1, 1, 1]` unmoved.**

## Unit 4 — `sdd-verify` FAIL closure: the spec and the code agree (PR 4)

**Status: [x]** — did not exist when tasks.md was written; added retroactively per the `sdd-verify` FAIL report (1 CRITICAL, 3 WARNING, 1 SUGGESTION).

- [x] 4.1 CRITICAL: `contract-input-partition/spec.md`'s "Requirement: A Produced-Fact Dependency Is An Internal-Chain Row" promised a mechanism (`PRODUCER_CHAIN_ABSENT` when a consumer's own `### Internal chain` table carries no row naming its produced-fact's producer) that `paper_graph._verify_producer_reachability` never implemented — it only proved graph reachability, never row presence. **Decision: implement the mechanism** (`_verify_producer_chain_rows`, `paper_graph.py`, called from `assemble_corpus` right after reachability), not amend the spec down — this is the exact defect class the whole change exists to close, and the real corpus already carries a row for every other producer/consumer pair, proving the discipline is practiced, not aspirational. RED-first: the decisive test (`ProducerChainRowsTests.test_a_direct_edge_with_the_row_absent_refuses`) reproduced the gap live — a real `after` edge reaching the consumer, row absent, assembled CLEAN before this fix. Mutation: `test_removing_the_check_flips_the_refusal_test_from_green_to_red` (new call site) and `test_mutation_removing_the_row_after_it_existed_is_caught` (the spec's own named scenario — same `requires_facts`, row deleted, re-assembly refuses; the row set is read live, never cached).
- [x] 4.2 Measured the real corpus against the new check: exactly ONE gap, `abstract.slot-2` requiring `contributions` (produced by `introduction.block-4b`) reachable only transitively, with no row. Fixed with one real `after` edge (reusing the block's own already-anchored quote) and one `### Internal chain` row in `sections/08-abstract.md`. Updated `tests/test_paper_contract.py`'s `PRE_MIGRATION_BODY_DIGESTS["08-abstract.md"]` to the new, genuine body digest (documented as a ruling-sanctioned prose change, matching the file's own convention for 02/03/05/06/07/09). Waves unmoved: `[22, 7, 11, 2, 2, 1, 1, 1]`.
- [x] 4.3 Adjusted `ProducerReachabilityTests` fixtures for the new stricter check: `_produces_facts_body` gained an optional `chain_rows` parameter (default empty, byte-identical for every unaffected caller); the shared `_write_coupling_sections`/`_write_optional_contribution_section`/`_write_optional_contribution_section`-style fixtures across `EvidenceTests`/`ReportShapeTests`/`CouplingOneTests`/`CouplingTwoTests`/`ArtefactsTests`/`FutureWorkTests`/`GapTests`/`OptionalVerifyTests` now carry real rows for every producer/consumer pair they declare. `test_transitive_reachability_through_an_intermediate_block_is_accepted` and a new `test_unreachable_refuses_at_the_reachability_layer_in_isolation` now call `_verify_producer_reachability` directly on a hand-built `Corpus` (this file's own direct-`BlockRecord` precedent), isolating that function's own transitivity leniency from the new, stricter, row-based documentation requirement layered on top of it — the two checks are no longer separable through the full `assemble_corpus` pipeline on a totally-unreachable input (a row can never exist without its backing edge also making the consumer trivially reachable), so the reachability-removal mutation test was repointed at the isolated test.
- [x] 4.4 WARNING 1: `paper_cli.cmd_phases`'s wave-block dict literal never forwarded `blocked_on_produced`, even though `compute_phases` already resolves it through the same `paper_readiness.compute_readiness` call `cmd_readiness` uses. Fixed (`.get("blocked_on_produced", [])`, matching `declined_facts`/`stale_declines`'s own convention). New test `ReadinessProducedFactsTests.test_phases_carries_blocked_on_produced_wave_verify_finding_warning_1`, RED-first confirmed via `git stash`. Verified side by side against the real corpus: `phases` and `readiness --paper paper` report `blocked_on_produced` for the identical 12 blocks.
- [x] 4.5 WARNING 2: `SKILL.md` documented zero of `produces_facts`, the six new refusal codes, or `blocked_on_produced`, and its own wave-count claim ("five waves shaped 28/11/5/2/1") was stale. Added a "A fact is declared, or it is produced" section; added the five new refusal codes to `contract`'s table row and `PRODUCED_FACT_UNDECLARABLE` to `declare`'s; corrected the wave claim by MEASURING it fresh (`paper_cli.py phases`): eight waves, `22/7/11/2/2/1/1/1`.
- [x] 4.6 WARNING 3: `specs/fact-production/spec.md`'s "Mutation — deleting the only producer is caught" scenario and `tasks.md` 2.3/2.4 named `limitations.lim-closing` as `limitations`'s producer; the shipped corpus (corrected in `8a557af`) actually names `limitations.lim-proposal-items` (`lim-closing` only CONSUMES the limitations). Corrected both documents to the shipped reality.
- [x] 4.7 SUGGESTION: `design.md` Decision F claimed corroboration is checked "structurally (does a coupling-verification check name this exact pair?)" — the implemented check (`len(producer_ids) == 2 and fact_id in paper_verify.CHECKS`) verifies only that SOME coupling check exists for that fact id, never pair-identity. Corrected the comment to match the code, noting it is safe by construction today (both derivations re-read the same corpus) but does not itself prove pair-identity.
- [x] 4.8 Full suite green: `.venv/bin/python -m unittest tests.test_paper_contract tests.test_paper_writing tests.test_paper_decisions tests.test_paper_citation tests.test_paper_evidence tests.test_paper_figure tests.test_paper_lifecycle`. **824/824 OK** (817 baseline + 5 new `ProducerChainRowsTests` + 1 new isolated reachability test + 1 new `phases`/`blocked_on_produced` test). Roster re-measured: **133**, unchanged (`PRODUCER_CHAIN_ABSENT` gained a second raise site, no new code). `contract`/`order`/`phases`/`readiness --paper paper` all exit `ok`; `order` 47 blocks, no dangling edges; `phases` waves `[22, 7, 11, 2, 2, 1, 1, 1]` unmoved.
